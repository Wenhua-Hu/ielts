[build]
  base = "ielts_questions"
  # install uv then build with mkdocs
  command = "curl -LsSf https://astral.sh/uv/install.sh | sh && uv run mkdocs build"
  publish = "site"


[build.environment]
  PYTHON_VERSION = "3.13"
